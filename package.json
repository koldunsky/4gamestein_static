{
  "name": "4gamestein_static",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --open",
    "build": "rm -rf .cache && rm -rf dist &&  NODE_ENV=production parcel build index.html --no-cache && node_modules/html-inline/bin/cmd.js dist/index.html >> index.inline.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.7",
    "sass": "^1.14.3",
    "html-inline": "^1.2.0"
  }
}
